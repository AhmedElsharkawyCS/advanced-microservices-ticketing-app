apiVersion: v1
kind: Service
metadata:
  name: authentication
spec:
  type: ClusterIP # default value
  selector:
    app: authentication
  ports:
    - name: authentication
      protocol: TCP
      port: 3000
      targetPort: 3000
